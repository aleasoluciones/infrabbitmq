# -*- coding: utf-8 -*-

from doublex import *
from expects import *
from doublex_expects import *


with describe('rabbitmq client specs'):
    with before.each:
         pass
    with context('when initiazling the client'):
        with it('foo'):
            pass
